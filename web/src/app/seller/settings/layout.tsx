import { SellerSettingsLayout } from "@/features/seller";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <SellerSettingsLayout>{children}</SellerSettingsLayout>;
}
