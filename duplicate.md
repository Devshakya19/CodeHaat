# Codebase Duplication Report

This file documents all the duplicate code blocks, duplicate logic, and structurally identical files found across the `web/src` codebase. It was generated using `jscpd` (JavaScript Copy/Paste Detector).

**Overall Duplication Rate:** 10.2%
**Total Clones Found:** 73
**Total Duplicated Lines:** 1336 lines

We need to resolve these duplicates in the future following the **DRY (Don't Repeat Yourself)** principle by utilizing React Shared Components, Custom Hooks, and Utility Functions.

---

## 1. Authentication Pages (`features/auth`)
The authentication flows share massive amounts of UI layout and form logic.
- **`login.tsx` & `register.tsx` & `developer-register.tsx`**:
  - The form card layout, input styling, and background styling are 100% identical.
  - The social login buttons (Google/GitHub) and the separator (`<hr />`) are duplicated.
- **`forgot-password.tsx` & `reset-password.tsx`**:
  - Both share the same UI wrappers, success/error states, and button layouts.

**Future Fix:** Create shared UI components (e.g., `AuthLayout`, `AuthInput`, `SocialLoginButtons`).

## 2. Navbars (`features/dashboard` & `features/seller`)
- ~~**`dashboard-navbar.tsx` vs `seller-navbar.tsx`**:~~
  - ~~The dropdown menu logic for the Profile avatar...~~
**Fix Applied:** Extracted a central `navbar.tsx` handling variants.

## 3. Profile Pages Logic (`profile.tsx`)
- ~~**`features/dashboard/pages/profile.tsx` & `features/seller/pages/profile.tsx`**:~~
  - ~~Even though the UI has been visually differentiated, the underlying React logic is completely duplicated.~~
**Fix Applied:** Created a custom React Hook `useProfile` in `hooks/profile.ts` and extracted a shared `ProfileSettings` component.

## 4. Wallet and Earnings (`features/seller`)
- **`earnings.tsx` vs `wallet.tsx`**:
  - The data fetching structure (`fetchData` wrapping multiple `apiGet` calls).
  - The Transaction/History list rendering logic (mapping over arrays, rendering `ArrowUpRight`/`ArrowDownLeft` depending on transaction type).

**Future Fix:** Extract a `TransactionList` shared UI component.

## 5. Shared Libraries (`shared/lib/api.ts` & `shared/lib/auth.ts`)
- **`api.ts`**:
  - `apiGet` and `apiPost` share the exact same `try/catch` wrapper and token retrieval logic.
- **`auth.ts`**:
  - `getUser` and `verifyToken` share repetitive header passing logic and error fallback returns.

**Future Fix:** Create a central `fetchWrapper` or `baseApiRequest` function that all other `apiGet`, `apiPost`, `apiPut`, `apiDelete` functions call.

## 6. Miscellaneous UI Components
- ~~**`cart-popup.tsx` vs `notification-popup.tsx` vs `wallet-popup.tsx`**:~~
  - ~~Both share the exact same absolute positioning popup UI, empty state layout (icon in the center with text).~~
**Fix Applied:** Created a central `PopupWrapper` component to handle layouts and backdrop logic.

---
*Generated by Antigravity*
