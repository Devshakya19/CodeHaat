import { PayoutSettings } from "@/features/seller/components/payout-settings";

export default function PayoutsSettingsPage() {
  return (
    <div className="w-full">
      <PayoutSettings />
    </div>
  );
}
